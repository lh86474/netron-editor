2026-06-05

Tags:  [[ambarella]]
## 6-5-26

### Notes

dynamically change the attributes. 
Don't modify the original graph
Some operation in the GUI, keep the original graph
- Export a new graph
- Can the GUI just accept the change on fly, best thing, but if not, support export new files

- One side original graph, one side modified graph. 
- real-time showing that this is the modified graph. 

extract part of a graph, annotate in GUI, and I can just extract it and not have anything else. 

Do processing only on a few of these nodes
- Only run part of the network. 
- Isolate a problem: isolate part of the graph. 
	- deletes all of the other things in the graph (on another side, with the original copy)
- Dynamically change properties, like add a property to link, add property to node, etc. 


